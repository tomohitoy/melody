<h1>Listing checkpoints</h1>

<table>
  <tr>
    <th>Mission</th>
    <th>Point</th>
    <th>Address</th>
    <th>Latitude</th>
    <th>Longitude</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @checkpoints.each do |checkpoint| %>
  <tr>
    <td><%= checkpoint.mission_id %></td>
    <td><%= checkpoint.point %></td>
    <td><%= checkpoint.address %></td>
    <td><%= checkpoint.latitude %></td>
    <td><%= checkpoint.longitude %></td>
    <td><%= link_to 'Show', checkpoint %></td>
    <td><%= link_to 'Edit', edit_checkpoint_path(checkpoint) %></td>
    <td><%= link_to 'Destroy', checkpoint, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Checkpoint', new_checkpoint_path %>
